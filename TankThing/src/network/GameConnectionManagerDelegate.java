package network;

public interface GameConnectionManagerDelegate {

	/* 
	 * this interface allows a connection to the GameConnectionManager.
	 * it specifies functions that it must implement in order to receive callbacks 
	 */
	
}
